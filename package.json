{
  "name": "fitcraft-monorepo",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=backend && npm run build --workspace=frontend",
    "dev": "node scripts/setup.js && concurrently \"npm run dev --workspace=backend\" \"npm run dev --workspace=frontend\"",
    "lint": "npm run lint --workspace=frontend",
    "postinstall": "node scripts/setup.js"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  },
  "packageManager": "npm@10.9.2"
}
